import * as auth from "./endpoints/auth";
import * as player from "./endpoints/player";
import * as packs from "./endpoints/packs";
import * as inventory from "./endpoints/inventory";
import * as quests from "./endpoints/quests";

export const api = {
  auth,
  player,
  packs,
  inventory,
  quests,
};
